<section class="section customer color-{{ section.settings.color_scheme }}" style="--pt: 64px; --pb: 64px;">
  <div class="customer__box">
    <h1 class="h2 text-center">{{ 'customer.reset_password.title' | t }}</h1>
    <p class="text-center">{{ 'customer.reset_password.subtext' | t }}</p>
    {%- form 'reset_customer_password', class: 'customer__form' -%}
      {%- if form.errors -%}<div class="form-message form-message--error">{{ form.errors | default_errors }}</div>{%- endif -%}
      <label class="field"><span class="field__label">{{ 'customer.login_page.password' | t }}</span><input class="field__input" type="password" name="customer[password]" autocomplete="new-password" required></label>
      <label class="field"><span class="field__label">{{ 'customer.reset_password.password_confirm' | t }}</span><input class="field__input" type="password" name="customer[password_confirmation]" autocomplete="new-password" required></label>
      <button type="submit" class="button button--full">{{ 'customer.reset_password.submit' | t }}</button>
    {%- endform -%}
  </div>
</section>

{% schema %}
{
  "name": "Redefinir senha",
  "settings": [ { "type": "color_scheme", "id": "color_scheme", "label": "Esquema de cores", "default": "scheme-2" } ]
}
{% endschema %}
